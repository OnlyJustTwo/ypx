$(function(){
    $("#bfECalendar_date").css({"position":" relative;","z-index":"99"})
    $("#bfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){}//回调函数
    });
    $("#afECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#cfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#dfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#efECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#ffECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#gfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#hfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#ifECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#jfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#lfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#mfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#nfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#ofECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#pfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#qfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#rfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#sfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#tfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#ufECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#vfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#wfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#xfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#yfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#zfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#aafECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#abfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#acfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#adfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#aefECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#affECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#agfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#ahfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#aifECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#ajfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#akfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#alfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#amfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#anfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#aofECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#apfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#aqfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#arfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#asfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#atfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#aufECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#avfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#awfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#axfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#ayfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#azfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#bafECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#bbfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#bcfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#bdfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#befECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#bffECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#bhfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#bifECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#bjfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#bkfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#blfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#bmfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#bnfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#bofECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#bpfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#bqfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#brfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#bsfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#btfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#bufECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#bvfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#bwfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#bxfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#byfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#bzfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#cafECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#cbfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#ccfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#cdfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#cefECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#cffECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#cgfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#chfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#cifECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#cjfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#ckfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#clfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#cmfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#cnfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#cofECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#cpfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#cqfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
    $("#crfECalendar_date").ECalendar({
        type:"time",   //模式，time: 带时间选择; date: 不带时间选择;
        stamp : false,   //是否转成时间戳，默认true;
        offset:[0,2],   //弹框手动偏移量;
        format:"yyyy年mm月dd日hh时ii分",   //时间格式 默认 yyyy-mm-dd hh:ii;
        skin:0,   //皮肤颜色，默认随机，可选值：0-8,或者直接标注颜色值;
        step:10,   //选择时间分钟的精确度;
        callback:function(v,e){} //回调函数
    });
})